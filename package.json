{
  "name": "mutation-test",
  "version": "1.0.0",
  "description": "Mutation test lab",
  "main": "src/index.js",
  "scripts": {
    "test": "npm run unit-test && npm run mutation-test",
    "unit-test": "npx jest --coverage",
    "mutation-test": "npx stryker run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@stryker-mutator/core": "^1.0.2",
    "@stryker-mutator/html-reporter": "^1.0.2",
    "@stryker-mutator/javascript-mutator": "^1.0.2",
    "@stryker-mutator/jest-runner": "^1.0.2",
    "stryker-html-reporter": "^0.18.1",
    "stryker-javascript-mutator": "^0.14.1",
    "stryker-jest-runner": "^1.4.1"
  },
  "dependencies": {
    "jest": "^24.1.0"
  }
}
